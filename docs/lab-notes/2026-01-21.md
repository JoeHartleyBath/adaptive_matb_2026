# Lab notes — 2026-01-21

## Goals

- Make OpenMATB setup/run docs match repo behavior
- Keep logging/output paths compliant with repo data boundaries

## Work done

- Audited OpenMATB Windows setup/run instructions against current wrapper + OpenMATB constants/logger
- Updated setup instructions to recommend the wrapper entrypoint (`run_openmatb.py`) for required env vars and output layout
- Clarified output locations and added mention of per-run `.manifest.json`
- Merged the OpenMATB setup/run doc update to `main` (PR #12)
- Tried to inspect OpenMATB output paths via `from core.constants import PATHS` and confirmed it fails without the OpenMATB dependencies installed (see Issues / risks)

Other work today (fill in):
- 

## Results / observations

- Running `python main.py` directly is fragile in this repo because the logger requires `OPENMATB_REPO_COMMIT` and `OPENMATB_SUBMODULE_COMMIT`.
- Wrapper-driven runs are the supported path and ensure logs land under the external data root.
- Importing OpenMATB core modules requires dependencies like `pyglet`; quick one-liners for introspection should be run inside the OpenMATB venv.
- Smoke test run completed successfully (via wrapper): window/tasks load and a session CSV + `.manifest.json` are written under the external output root.
  - Run details (fill in): participant= , session= , scenario= , output_root= 

## Decisions (link ADRs)

- Related ADRs:
  - [ADR-0001: Repository structure and data boundaries](../decisions/ADR-0001-repo-structure.md)
- New decisions to capture as ADRs:
  - None

## Issues / risks

- Windows installs may fail on `pyparallel`; the docs keep a pragmatic workaround, but parallel-port triggers will be unavailable.
- `python -c "from core.constants import PATHS"` fails with `ModuleNotFoundError: No module named 'pyglet'` if the OpenMATB venv is not active / deps not installed.

## Next actions

Plan calibration phase for the study (tomorrow):

1) Write the “model input contract” (must be stable)

- Decide (on paper):
  - Sampling rate (what we will train on; what we will record)
  - Channel set / montage (how many channels; how missing channels are handled)
  - Window length and stride (e.g., 2s windows every 0.25s)
- Preprocessing that must be identical across public + our data:
  - Filtering (bandpass/notch)
  - Referencing
  - Normalisation (per-session, per-subject, global)
  - Artefact handling (none online / simple rejection / ASR offline)
- Label definition for MWL during calibration:
  - Difficulty level as primary label; performance as a check
- Deliverable: single-page spec (anchor for everything)

2) Choose public datasets based on compatibility with the contract

- Criteria: raw EEG (not only features), workload labels/difficulty, compatible sampling rate, sufficient channel count, clear segmentation
- Deliverable: shortlist of 2–3 datasets + rationale

3) Define calibration protocol for fine-tuning (10 people)

- Design goals: generate labels; cover target MWL range; short enough to run; long enough to learn
- Default v0: 3 difficulty levels (low/med/high), 2–3 min each, ramp order low→med→high, continuous performance logging
- Deliverable: exact block structure + event markers to log

4) Decide fine-tuning strategy class (minimum viable)

- Options: last layer only vs last block + head vs full fine-tune
- Default: fine-tune classifier head only + per-subject normalisation
- Deliverable: written decision + fallback if calibration fails

5) Closed-loop adaptation policy (keep it boring)

- Start with: hysteresis + dwell + cooldown; bounded difficulty changes; logged decisions
- Deliverable: policy written in plain English (no parameter tuning yet)

If needed, (re)install deps in the OpenMATB venv: `python -m pip install -r requirements.txt`

## Time spent (optional)

- Total:
- Breakdown:
  - Docs audit + patch:
