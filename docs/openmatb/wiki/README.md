# OpenMATB wiki mirror

These files are a **local snapshot** of the upstream OpenMATB GitHub wiki:
- Upstream source: https://github.com/juliencegarra/OpenMATB/wiki
- Submodule in use: `src/python/vendor/openmatb/` (pinned to a specific commit — see `.gitmodules`)

**Last synced:** 2026-01-22 (approximate; during initial repo setup)

## Usage

Use these pages as offline reference for scenario syntax, plugin parameters, and configuration options.

## Important caveats

- This snapshot may **not match** the current state of the upstream wiki or the pinned submodule.
- If the submodule is updated, manually re-check these pages for breaking changes to syntax or plugin behaviour.
- Do **not** treat these files as authoritative over the actual pinned submodule source code — when in doubt, read the plugin source directly under `src/python/vendor/openmatb/plugins/`.
